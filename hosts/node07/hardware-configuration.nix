# PLACEHOLDER — replace with this node's real hardware config.
#
# On the freshly installed node, run:
#   nixos-generate-config --show-hardware-config > hardware-configuration.nix
# then copy that file here (hosts/node07/) and commit it.
throw "hosts/node07/hardware-configuration.nix is a placeholder. Replace it with the output of `nixos-generate-config --show-hardware-config` from node07 (see README step 3)."
